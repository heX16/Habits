

# Kivy version


## 1

"Старый notification_service больше не используется (остался только в документации)." - почисти доку.
"очистим старый notification_service.py файл... Теперь давайте удалим старый notification_service.py и улучшим" - надо удалить старые файлы.


Я вижу файл "notification_popup.py". Кажется его тоже нужно удалить, но там два файла, убедись, что ты удаляешь тот, который не актуальный.
notification_manager.py
notification_popup.py


## 2

`def get_database_info(self) -> dict:`
`'db_version': self.habits_model.database.DB_VERSION`
thats not right.
use a varible that is stored  in the database itself.
its a `VALID_GLOBAL_PARAMS`, `'db_version',    # Database version`.




## def get_status_lists(self):

```
def get_status_lists(self):

        # TODO: (читай текст ниже...)
        # идея для оптимизации:
        # нужно сделать один массив который содержит все состояния и их описания (цвет, иконка и тп).
        # а эта функция будет возвращать наборы из словарей вида:
        # 'gh1': {
        #     HabitStatus.NOT_SET: 0,
        #     HabitStatus.DONE: 1,
        #     HabitStatus.FAIL: 2
        # }, ...
        # тоесть словари, которые указывают на номера в массиве. а также их ключи - это номер статуса.

```

Не пиши код сразу, сначала давай подумаем над задачей, сделай какой-то анализ, предложение, вообще что нужно сделать, что ты собираешься сделать, я почитаю и уже дальше будем думать и работать.


## def _setup_midnight_check(self):

функционал "проверки полночи и обновления страницы в полночь" не имеет смысла в нативном приложении в приложении.
это только для браузера.

Но при этом в нативном приложении имеет смысл делать другие проверки.
1. когда приложение было снято с паузы, нужно сразу проверять дату модификации DB, чтобы отследить, что файл изменился.

2. нужно следить за изменением DB когда приложение активно. Приложение открывает DB, загружает оттуда данные и сразу же закрывает DB. Это нужно чтобы DB не была защищена от записи (другое приложение может туда писать). И приложение должно следить за тем, что файл изменился. То есть нужно вешать какой-то нотификатор. Это должно быть не отслеживание изменений времени, а именно штука, которая следит, что файл изменился. То есть это системный notification о изменении файла. Если такой системный notification недоступен, то тогда вешать проверку по времени - например раз в минуту проверять дату модификации файла.


`def _trigger_midnight_refresh(self):`
`def _check_midnight(self, dt):`

```
                if (now - pause_time).total_seconds() > 4 * 3600 or now.date() != pause_time.date():
                    Logger.info('AppStateManager: Long pause detected, triggering refresh')
                    self._trigger_midnight_refresh()
```

Не пиши код сразу, сначала давай подумаем над задачей, сделай какой-то анализ, предложение, вообще что нужно сделать, что ты собираешься сделать, я почитаю и уже дальше будем думать и работать.

